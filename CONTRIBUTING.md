# 貢獻指南 Contributing

謝謝你願意幫忙！這是一個**免費、繁體中文的 AI 工具入門指南集**。本專案的核心價值是 **準確、附原始來源、夠新**——任何貢獻都以此為最高原則。

## 我最需要的幫忙

1. **回報過時／錯誤**（最歡迎）：AI 工具的價格、功能、模型版本變很快。如果你發現哪篇內容過時、連結失效、或事實有誤，請告訴我。
2. **修錯字／改善用字**：繁體中文的錯字、語句不順、術語不一致。
3. **補充或新增指南**：想補一個主題、或新增一篇工具指南（請先看下方格式與原則）。

## 怎麼回報（不用會寫程式）

- **想討論／提問／回報過時** → 開 [Discussions](../../discussions)（像留言區，最輕鬆）。
- **明確的錯誤／待修項** → 開 [Issue](../../issues)，標題寫清楚是哪一頁、哪一段。
- 回報時請附上：**哪一篇、哪一段、正確資訊的來源連結**（越具體越快修）。

## 內容原則（投稿／改稿必讀）

- **以官方／原始論文為權威骨幹**：每個事實盡量能追到第一手來源（官方文件、原始論文）。二手資料只作交叉佐證。
- **衝突數據標註分歧**：不同來源數字不一致時，採第一手、並註明分歧，不擅自取一個。
- **不誇大、不臆測**：條件不足就說「未能確認」，不要編。
- **語言**：預設繁體中文；技術術語／指令／路徑／程式碼保留英文。
- **語氣**：給初學者看，清楚、白話、不嚇人。

## 新增／修改一篇指南的格式

本站每篇指南共用同一套設計系統（深色 hero + 卡片 + 列印樣式 + ⌘K 搜尋 + 深色模式）。請**沿用既有結構**，不要自創排版：

- 從現有 `guides/*.html` 複製一份當骨架，或用同源的頁面模板。
- 結構固定：Hero → 目錄 → 總覽 → 各工具/章節 → 比較 → 決策 → 起步 → 來源/資料邊界 → footer。
- `<head>` 需含：`<title>`、`description`、`canonical`、Open Graph、Twitter card、`last-updated` / `last-verified` 兩個 `<meta>`。
- 共用樣式與腳本走 `assets/site.css`、`assets/site.js`（**不要把這些 inline 回各頁**）。
- 顏色一律用 CSS 變數（`:root` tokens），不要寫死 hex（深色模式靠 token 切換）。

## 改完別忘了（維護步驟）

1. 更新該頁 `<meta name="last-verified">` 為今天；若內容有實質變動，另更新 `last-updated` 並寫 `CHANGELOG.md`。
2. **搜尋索引（`pagefind/`）與 `sitemap.xml` 已自動化** —— push 到 `main` 後 GitHub Actions 會自動重建並 commit，**不用手動跑**（詳見 `.github/workflows/reindex.yml`）。
3. 詳見 `MAINTENANCE.md`。

## 不在範圍內

- 把站改成需要後端／資料庫的服務（本站刻意維持純靜態）。
- 與「AI 工具入門教學」無關的內容。
- 失去中立性的業配／聯盟推銷。

## 授權

貢獻即表示你同意你的內容以本專案的 [MIT License](LICENSE) 釋出。

---

有任何不確定，先到 [Discussions](../../discussions) 問一聲就好，謝謝你！
